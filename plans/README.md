# Animation improvement plans

Generated by `/improve-animations` from the `/review-animations` and `/find-animation-opportunities` passes run earlier in this session (commit `bf245a3`). All 15 candidates were approved for planning ("all of them").

Two plans (010, 011, 012) bake in a default decision on a product/behavior question that was left open in the audit — each states its assumption explicitly at the top ("Decision this plan bakes in"). If you disagree with the assumption, don't execute that plan; say what you'd rather do instead and a replacement plan can be written.

## Plans

| # | Title | Severity | Status | File overlap notes |
| --- | --- | --- | --- | --- |
| 001 | Fix ParticleBackground ignoring prefers-reduced-motion | HIGH | TODO | Isolated (`ParticleBackground.tsx`) |
| 002 | Move hero glow cursor-follow off layout properties | HIGH | TODO | `HomeSection.tsx` — same file as 003, 010, 013 |
| 003 | Rewrite text marker-highlight to use transform | HIGH | TODO | `HomeSection.tsx` — same file as 002, 010, 013 |
| 004 | Cache letter rects, rAF-batch ProximityWaveHeading | HIGH | TODO | Isolated (`ProximityWaveHeading.tsx`) |
| 005 | Fix TopNav conflicting transition utilities | MEDIUM | TODO | `TopNav.tsx` — same file as 013 (partial), 014 |
| 006 | Replace ProgressRail width/height with scale | MEDIUM | TODO | Isolated (`ProgressRail.tsx`) |
| 007 | Cap hackathon-tile / pub-card hover durations | MEDIUM | TODO | Depends on 008 landing first (see below) |
| 008 | Replace `transition: all` with named properties (7 sites) | MEDIUM | TODO | Should land **before** 007 |
| 009 | Gate all `:hover` motion behind `(hover: hover)` | LOW | TODO | `index.css` only |
| 010 | Convert parallax off the Framer Motion `y` shorthand | HIGH | TODO | 4 files incl. `HomeSection.tsx` — land after 002/003 |
| 011 | Fire hackathon confetti on click, not hover | MEDIUM | TODO | `HackathonsSection.tsx` |
| 012 | Stop Skills autoplay after manual interaction | MEDIUM | TODO | `SkillsSection.tsx` |
| 013 | Add press feedback to primary CTA buttons | opportunity | TODO | `HomeSection.tsx` (land after 002/003/010), `TopNav.tsx` |
| 014 | Add sliding active-section indicator to TopNav | opportunity | TODO | `TopNav.tsx` — depends on 005 landing first |
| 015 | Press feedback sweep (arrows, links, modal close) | opportunity | TODO | 5 files, all isolated from the above except minor overlap with 008's `transition-colors` sites |

## Recommended execution order

1. **001** — trivial, isolated, highest-severity bug fix. No dependencies.
2. **005** — trivial, isolated, fixes a real (if minor) bug. Do before 014 (which depends on it).
3. **006** — trivial, isolated.
4. **008** — mechanical sweep across 7 sites. Do before 007 (007's target durations are written against 008's cleaned-up property lists).
5. **007** — depends on 008.
6. **009** — mechanical, `index.css`-only, no code dependencies but touches many of the same selectors 007/008 touch; doing it after 007/008 avoids re-deriving property lists mid-edit.
7. **002** — moderate effort, `HomeSection.tsx`.
8. **003** — moderate-high effort, same file as 002; do after 002 to avoid overlapping edits to the same effect block.
9. **004** — isolated, can run any time after step 1; sequenced here for no particular reason other than grouping the performance-fix plans together.
10. **010** — touches `HomeSection.tsx` again (plus 3 other files); do after 002/003 so all of this file's mousemove-effect edits land before the JSX-only parallax edit.
11. **011** — isolated to `HackathonsSection.tsx`, no dependencies.
12. **012** — isolated to `SkillsSection.tsx`, no dependencies.
13. **013** — touches `HomeSection.tsx` (again) and `TopNav.tsx`; do after 002/003/010 (same-file edits) and either before or after 005 (no actual conflict, listed here for grouping).
14. **014** — depends on 005 (its target snippet assumes 005's fix is already applied); do after 013 only to keep `TopNav.tsx` edits grouped, not a hard dependency.
15. **015** — isolated from everything else; can run any time, listed last as cleanup/polish.

## Notes

- Plans 002, 003, 010, and 013 all touch `HomeSection.tsx`. None of their line-range edits overlap (002: lines 78-91 + CSS; 003: lines 94-197 + CSS; 010: lines 205-291 JSX only; 013: lines 233-244 JSX only), so they're safe to execute serially in the order above without conflicts, but each plan's file-state assumptions should be re-verified (re-read the file) immediately before executing it, since an earlier plan in this list will have already changed it.
- Plans 005 and 014 both touch `TopNav.tsx`; 014 explicitly depends on 005.
- Plans 007 and 008 both touch the same two files (`HackathonsSection.tsx`, `src/index.css`'s `.pub-card`/`.pub-card::before` rules) — 008 changes property lists, 007 changes durations on the same declarations; land 008 first as noted above.
- No plan here touches `src/data/*.ts` or any copy/content file — per the original instruction, this is purely a motion-quality pass; every plan's Boundaries section explicitly excludes content/data changes.
